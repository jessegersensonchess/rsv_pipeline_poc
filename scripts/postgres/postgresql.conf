# postgresql.conf
log_statement = 'all'  # Log all SQL statements
log_error_verbosity = 'verbose'  # Detailed error messages
log_destination = 'stderr'  # Ensure logs go to stderr for Docker to capture

